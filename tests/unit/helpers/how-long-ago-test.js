import { howLongAgo } from 'ember-qn-a/helpers/how-long-ago';
import { module, test } from 'qunit';

module('Unit | Helper | how long ago');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = howLongAgo([42]);
  assert.ok(result);
});
