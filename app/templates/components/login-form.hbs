<h1>Log in, and you'll no longer be anonymous!</h1>
<div>
	<form>
		<div class="form-group">
		<label for="userName">UserName:</label>
		{{input value=userName id="userName" class="form-control"}}
		</div>
		<button class="btn btn-primary outline" {{action "logIn"}}>Click here to log in</button>
	</form>

</div>

